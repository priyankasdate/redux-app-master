import React from 'react'
import { useSelector } from "react-redux"

const FirstComp = () => {
    // debugger;
    // const name = useSelector((state) => state.userReducer.users[0]);
    return (
        <div>FirstComp</div>
    )
}

export default FirstComp